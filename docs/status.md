# ARC3 Runner 当前状态

更新时间：2026-07-25

## 当前里程碑

ARC3 实验室已完成单仓收敛。`arc3-runner` 是唯一活跃入口，统一承载可运行产品、符号数学包、世界模型实验、部署源文件和研究资料。收敛基线提交为 `b8524e8`。

## 已完成验收

- 产品测试：27 项通过。
- `packages/arc3-math`：13 项通过。
- `experiments/world-model`：7 项通过。
- Python lint、前端 ESLint、TypeScript 与 Vite 构建通过。
- npm 依赖审计为 0 个已知漏洞。
- 本地“打开体验”动作与重复打开复用逻辑通过真实浏览器验证。
- 运行界面的决策卡、想象/实际帧对比、训练趋势、episode 回放和帮助导览可用。

## 已知边界

- `deploy/arc3-wordm-us` 仅保留部署源和原有路由配置，本次没有执行线上部署。
- 训练数据库属于本地运行数据，不进入 Git；收敛前数据已单独归档。
- 测试仍显示一条 Starlette `TestClient` 迁移提醒，不影响当前通过状态。

## 下一步

1. 用更多官方环境扩充评测样本，持续校准置信度与 ECE。
2. 为数学包和世界模型定义晋升门槛，达到指标后再进入产品运行时。
3. 线上部署前单独审查 Cloudflare 配置、域名和回滚方案。
